$kind: http-request
name: DELETE Delete booking
url: "{{baseUrl}}/booking/{{validId}}"
method: DELETE
headers:
  Cookie: token={{token}}
scripts:
  - type: beforeRequest
    code: ""
    language: text/javascript
  - type: afterResponse
    code: "pm.test(\"Status code is 201\", () => {\r

      \    pm.response.to.have.status(201);\r

      });\r

      \r

      pm.test(\"Response body is 'Created'\", () => {\r

      \    pm.expect(pm.response.text()).to.equal(\"Created\");\r

      });\r

      \r

      pm.sendRequest(`${pm.environment.get(\"baseUrl\")}/booking/${pm.collectio\
      nVariables.get(\"validId\")}`, (err, res) => {\r

      \    pm.test(\"Booking should no longer exist\", () => {\r

      \        pm.expect(res.code).to.equal(404);\r

      \    });\r

      });"
    language: text/javascript
order: 4494330226157348
